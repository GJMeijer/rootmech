% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_weibull.R, R/power_weibull_OLD.R
\name{power_weibull_loglikelihood}
\alias{power_weibull_loglikelihood}
\title{Likelihood function for power-law weibull loglikelihood fitting}
\usage{
power_weibull_loglikelihood(par, lx, ly, weights = rep(1, length(lx)))

power_weibull_loglikelihood(par, lx, ly, weights = rep(1, length(lx)))
}
\arguments{
\item{par}{fitting parameter vector: log(multiplier), power, log(shape).}

\item{lx, ly}{vectors with log-transformed x and y measurements}

\item{weights}{weighting for each measurement}
}
\value{
log-likelihood score

log-likelihood score
}
\description{
Calculates the negative loglikelihood of the power-law + weibull shape
fit. Multiplier and weibull shape parameters are inputted as log-
transformed values to avoid issues with negative numbers.

Calculates the negative loglikelihood of the power-law + weibull shape
fit. Multiplier and weibull shape parameters are inputted as log-
transformed values to avoid issues with negative numbers.
}
